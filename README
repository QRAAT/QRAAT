Code for the RMG project.

installation instructions:

./build-gnuradio
<Will take about an hour for a full download/build/install>

vi ~/.bashrc
<PYTHONPATH to .bashrc, as suggested at the end of the last script>

./install-rmg install
<build/install Todd's code>

  After installation, the following scripts are made available 
  from the command line:
   4channel_spectrum
   4channel_specturm_{serv,client}
   tx_setup
   run_rmg
   read_det


